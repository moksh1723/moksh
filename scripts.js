let input = document.getElemenntById('inputbox');
let buttons = document.querySelectorALL
('button');
let string ="";
let arr = Array.from(buttons);
arr.forEach(button => {
button.addEventListener('click', (e) =>){
    if (e.target.inner)
}
})